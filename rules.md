Bounded concurrency. Process jobs through a background worker pipeline that runs at most N model calls concurrently across the whole service (make N configurable, default 3). Extra work must queue and wait — it must not all fire at once. 2. Chunking + reassembly. Documents that exceed the model context window must be split into chunks, summarized, and then combined into one coherent final summary (a simple map-reduce: summarize each chunk, then summarize the summaries). Token counts and cost must aggregate across all chunks. 3. Resilient model calls. Handle transient failures (timeouts, rate limits, 5xx) with retry + backoff, and stop hammering a failing dependency (e.g. circuit breaker or a cap on attempts). A single failed chunk should fail the job with a clear reason — partial results must never be silently returned as if complete. 4. Priority. "high" priority jobs must be picked up before "normal" ones already waiting in the queue. 5. Cost & token logging. Log input/output tokens and approximate USD cost per chunk and per job, using structured logs (not string concatenation). 6. Input validation. Reject empty input, oversized input (> 50,000 words), wrong content type, and invalid priority values, with appropriate HTTP status codes and a clear error body. 7. README. Explain your design choices, your concurrency model, and a "What I would add with more time" section. 